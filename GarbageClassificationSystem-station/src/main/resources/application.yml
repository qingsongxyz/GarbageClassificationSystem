spring:
  mvc:
    throw-exception-if-no-handler-found: true

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: debug
        cloud:
          openfeign: debug
          sleuth: debug


